# 速率限制处理手动测试指南

## 概述

本指南描述如何手动验证系统正确处理 HTTP 429（Too Many Requests）速率限制错误。

## 测试目标

验证以下行为：
1. 429 错误被识别为可重试错误
2. 429 错误触发自动重试机制
3. 重试使用指数退避（1s → 2s → 4s → ...）
4. 重试次数达到上限后失败
5. 用户看到友好的错误消息
6. 队列管理器正确处理 429 错误

## 前置条件

- 后端服务运行中
- 数据库已初始化
- 有效的 Anthropic API 密钥（或模拟模式）

## 测试场景

### 场景 1: 429 错误分类验证

**目标**: 验证 429 错误被正确分类为 `rate_limit_error`

**步骤**:
1. 运行自动测试脚本:
   ```bash
   cd backend
   npx tsx tests/verify-rate-limit-handling.ts
   ```

2. 检查输出中的 "Test 429 Status Code is Retryable" 测试

**预期结果**:
- ✅ 429 状态码被识别为可重试
- ✅ 错误类型为 `rate_limit_error`
- ✅ `isRetryable()` 返回 `true`

---

### 场景 2: 429 错误重试行为

**目标**: 验证收到 429 响应时自动重试

**步骤**:
1. 使用快速验证脚本:
   ```bash
   cd backend
   node tests/verify-rate-limit-simple.js
   ```

2. 观察测试输出

**预期结果**:
- ✅ 第一次请求收到 429
- ✅ 等待约 100ms（初始延迟）
- ✅ 第二次请求收到 429
- ✅ 等待约 200ms（指数退避）
- ✅ 第三次请求成功（或最终失败）

**时间序列示例**:
```
00:00.000 - Attempt 1: 429 Too Many Requests
00:00.100 - [Retry after 100ms]
00:00.100 - Attempt 2: 429 Too Many Requests
00:00.300 - [Retry after 200ms]
00:00.300 - Attempt 3: Success
```

---

### 场景 3: 指数退避验证

**目标**: 验证重试延迟遵循指数增长模式

**步骤**:
1. 运行完整测试套件:
   ```bash
   cd backend
   npx tsx tests/verify-rate-limit-handling.ts
   ```

2. 查找 "Test 429 Error Triggers Retry with Exponential Backoff" 测试

**预期延迟序列**:
- 尝试 1 → 尝试 2: ~1000ms（初始延迟）
- 尝试 2 → 尝试 3: ~2000ms（2x）
- 尝试 3 → 尝试 4: ~4000ms（4x）
- 最大延迟限制: 10000ms

**带抖动的延迟**（jitter=true）:
- 允许 ±25% 的随机变化
- 例如: 1000ms 可能在 750-1250ms 之间

---

### 场景 4: 最大重试次数限制

**目标**: 验证系统不会无限重试 429 错误

**步骤**:
1. 查看配置文件 `backend/.env.example`
2. 确认 `CLAUDE_MAX_RETRY_ATTEMPTS=3`
3. 运行测试模拟持续 429 错误

**预期行为**:
- ✅ 尝试 1: 429 错误
- ✅ 尝试 2: 429 错误（重试 1）
- ✅ 尝试 3: 429 错误（重试 2）
- ✅ 尝试 4: 429 错误（重试 3，最后一次）
- ✅ 最终失败，抛出错误
- ✅ 总共 3 次重试（4 次尝试）

**不应发生**:
- ❌ 无限重试
- ❌ 超过配置的最大次数
- ❌ 立即失败（不重试）

---

### 场景 5: 用户友好的错误消息

**目标**: 验证用户看到中文友好提示

**步骤**:
1. 检查 `backend/src/utils/errors.ts` 中的 `USER_MESSAGES`
2. 查找 `ErrorType.RATE_LIMIT` 的消息定义

**预期内容**:
```typescript
[ErrorType.RATE_LIMIT]: {
  title: '请求过于频繁',
  message: 'API 请求频率超限，系统正在自动重试',
  suggestion: '请稍等片刻，系统会自动处理',
}
```

**验证点**:
- ✅ 标题简洁明了
- ✅ 消息说明系统正在自动重试
- ✅ 建议用户等待
- ✅ 所有文本为中文

---

### 场景 6: 队列任务重试逻辑

**目标**: 验证队列管理器对 429 错误的处理

**前置条件**: 需要实际运行后端服务和队列

**步骤**:
1. 启动后端服务:
   ```bash
   cd backend
   npm run dev
   ```

2. 在另一个终端，触发一个会收到 429 的任务（例如，频繁创建笔记）

3. 监控数据库中的任务状态:
   ```bash
   cd backend
   npx prisma studio
   # 或使用 SQLite 客户端查看
   ```

4. 查询任务重试信息:
   ```sql
   SELECT id, status, retryCount, lastRetryAt, nextRetryAt, error
   FROM ClaudeTask
   WHERE status = 'pending' OR status = 'failed'
   ORDER BY createdAt DESC
   LIMIT 5;
   ```

**预期数据库状态**:
- `retryCount` 每次重试增加 1
- `lastRetryAt` 记录上次重试时间
- `nextRetryAt` 设置为未来的重试时间（指数退避）
- `status` 为 'pending'（等待重试）或 'failed'（超过最大次数）

**预期 SSE 事件**:
- `task.retry` - 任务被调度重试
- 包含: `retryCount`, `nextRetryAt`, `error`

---

### 场景 7: 429 与其他状态码的对比

**目标**: 验证 429 的处理与其他错误不同

**对比测试**:

| 状态码 | 类型 | 可重试 | 行为 |
|--------|------|--------|------|
| 429 | rate_limit_error | ✅ | 指数退避重试 |
| 401 | authentication_error | ❌ | 立即失败 |
| 403 | authentication_error | ❌ | 立即失败 |
| 500 | server_error | ✅ | 指数退避重试 |
| 502 | server_error | ✅ | 指数退避重试 |
| 503 | server_error | ✅ | 指数退避重试 |
| 400 | client_error | ❌ | 立即失败 |

**验证命令**:
```bash
cd backend
npx tsx tests/verify-rate-limit-handling.ts
# 查找 "Test 429 vs Other Status Codes" 测试
```

---

## 自动化测试命令

### 快速验证
```bash
cd backend
node tests/verify-rate-limit-simple.js
```

### 完整测试套件
```bash
cd backend
npx tsx tests/verify-rate-limit-handling.ts
```

### 预期输出示例

**快速测试**:
```
🧪 Rate Limit Handling Quick Tests

✅ Test 1: 429 is retryable
✅ Test 2: 429 triggers retry with exponential backoff
✅ Test 3: 429 fails after max retries
✅ Test 4: 429 vs other status codes
✅ Test 5: Exponential backoff pattern
✅ Test 6: 429 does not fail immediately

============================================================
Results: 6/6 tests passed
============================================================

✅ ALL TESTS PASSED!
```

**完整测试**:
```
🧪 Starting Rate Limit Handling Tests...

Testing 429 Too Many Requests error handling
Verifying exponential backoff and retry behavior

✅ PASS: Test 429 Status Code is Retryable
  Duration: 5ms
  Message: 429 status correctly identified as retryable

✅ PASS: Test 429 Error is Classified as RATE_LIMIT
  Duration: 3ms
  Message: 429 error correctly classified as RATE_LIMIT and retryable
  Details: { type: 'rate_limit_error', statusCode: 429, retryable: true }

...

================================================================================
RATE LIMIT HANDLING TEST SUMMARY
================================================================================

Total Tests: 8
Passed: 8
Failed: 0
Success Rate: 100.0%

✅ ALL TESTS PASSED!
================================================================================
```

---

## 实际 API 测试（可选）

**注意**: 此测试需要真实的 Anthropic API 密钥和达到速率限制。

### 模拟速率限制

由于实际触发 API 速率限制较困难，建议使用模拟测试：

1. **修改 Claude 服务临时模拟 429**:
   ```typescript
   // backend/src/llm/claude.service.ts (临时)
   // 在某个方法中临时抛出 429 错误
   if (this.shouldSimulateRateLimit) {
     const error: any = new Error('Too many requests')
     error.status = 429
     throw error
   }
   ```

2. **观察日志**:
   - 查看重试消息
   - 确认指数退避延迟
   - 验证最终成功或失败

3. **恢复代码**:
   - 删除模拟代码
   - 确保生产代码不受影响

---

## 故障排查

### 问题 1: 测试失败 - "429 is not retryable"

**可能原因**:
- `isHttpStatusCodeRetryable` 函数未正确处理 429

**解决方案**:
- 检查 `backend/src/utils/retry.ts` 中的实现
- 确认包含: `if (statusCode === 429) return true`

### 问题 2: 重试延迟不符合预期

**可能原因**:
- 抖动（jitter）导致延迟在范围内变化
- 测量时间不准确

**解决方案**:
- 在测试中设置 `jitter: false`
- 使用高精度时间戳（`Date.now()` 或 `performance.now()`）
- 允许 ±5% 的测量误差

### 问题 3: 队列任务未重试

**可能原因**:
- `isRetryable` 错误分类
- 队列管理器未正确集成重试逻辑
- 数据库字段未更新

**解决方案**:
- 检查 `classifyError` 对 429 的分类
- 验证队列管理器的 `executeTask` 方法
- 确认数据库 schema 包含 `retryCount`, `lastRetryAt`, `nextRetryAt`

---

## 验证清单

完成以下检查项确保速率限制处理正确实现：

### 代码层面
- [ ] `isHttpStatusCodeRetryable(429)` 返回 `true`
- [ ] `classifyError` 将 429 分类为 `rate_limit_error`
- [ ] `isRetryable(classified429Error)` 返回 `true`
- [ ] 429 错误触发 `retryWithBackoff` 重试
- [ ] 重试延迟遵循指数退避模式
- [ ] 最大重试次数配置生效

### 用户体验
- [ ] 用户看到友好的中文错误提示
- [ ] 错误提示说明系统正在自动重试
- [ ] 用户知道应该等待系统自动处理

### 队列处理
- [ ] 429 错误的队列任务被标记为待重试
- [ ] `retryCount` 字段正确递增
- [ ] `nextRetryAt` 设置为未来的时间
- [ ] 最终失败的任务标记为 `FAILED`
- [ ] SSE 事件正确通知前端

### 测试覆盖
- [ ] 快速测试全部通过 (6/6)
- [ ] 完整测试套件全部通过 (8/8)
- [ ] 指数退避模式验证通过
- [ ] 最大重试次数限制生效
- [ ] 用户消息完整且友好

---

## 实现说明

### 当前实现

**重试策略**:
- 使用指数退避（1s → 2s → 4s → ...）
- 添加 ±25% 随机抖动避免惊群效应
- 最大延迟限制 10 秒
- 默认最大重试 3 次

**未实现**:
- ❌ Retry-After 响应头支持

**原因**:
Anthropic API 的 429 响应通常不包含 Retry-After 头，或者该头的值不准确。因此当前实现使用标准的指数退避策略。

### 未来改进

如果需要支持 Retry-After 头，可以增强重试逻辑：

```typescript
// 伪代码示例
function extractRetryAfter(error: any): number | null {
  const retryAfter = error.response?.headers?.['retry-after']
  if (!retryAfter) return null

  // Retry-After 可能是秒数或 HTTP-date
  const seconds = parseInt(retryAfter, 10)
  if (!isNaN(seconds)) {
    return seconds * 1000 // 转换为毫秒
  }

  // 解析 HTTP-date (例如: "Fri, 31 Dec 2024 23:59:59 GMT")
  const date = new Date(retryAfter)
  if (!isNaN(date.getTime())) {
    return date.getTime() - Date.now()
  }

  return null
}

// 在重试逻辑中使用
const retryAfterDelay = extractRetryAfter(error)
const delay = retryAfterDelay || calculateExponentialBackoff(attempt)
```

---

## 相关文件

- **重试逻辑**: `backend/src/utils/retry.ts`
- **错误分类**: `backend/src/utils/errors.ts`
- **Claude 服务**: `backend/src/llm/claude.service.ts`
- **队列管理**: `backend/src/queue/queue-manager.ts`
- **测试套件**: `backend/tests/verify-rate-limit-handling.ts`
- **快速测试**: `backend/tests/verify-rate-limit-simple.js`

---

## 总结

速率限制处理是确保系统稳定性和用户体验的重要功能。通过正确的错误分类、自动重试和指数退避，系统可以在遇到 API 速率限制时优雅地处理，而不是立即失败。

当前实现使用标准的指数退避策略，无需依赖 Retry-After 响应头，这使得系统更加健壮和可靠。
